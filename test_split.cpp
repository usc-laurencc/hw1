/*
CSCI 104: Homework 1 Problem 1

Use this file to test your split.cpp file.

Create linked lists and split them with your split() function.

You can compile this file like this:
g++ split.cpp test_split.cpp -o test_split
*/

#include "split.h"
#include <iostream>

using namespace std;

int main(int argc, char* argv[])
{
    //Node* four = new Node(412, NULL);
    //Node* three = new Node(3122, four);
    //Node* two = new Node(232, three);
    //Node* one = new Node(12, nullptr);

    Node* odds = nullptr;
    Node* evens = nullptr;

    //split(one, odds, evens);

    /*
    Node* temp = odds;
    cout << "ODDS: ";
    while (temp != NULL) {
        cout << temp->value << " -> ";
        temp = temp->next;
    }

    temp = evens;
    cout << endl << "EVENS: ";
    while (temp != NULL) {
        cout << temp->value << " -> ";
        temp = temp->next;
    }

    temp = in;
    cout << endl << "IN: ";
    while (temp != NULL) {
        cout << temp->value << " -> ";
        temp = temp->next;
    }
    */
    //delete four;
    //delete three;
    //delete two;
    delete one;
}
